apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: slackbot-stage.ara.decc.vmware.com
  name: slackbot-api-server-stage
  labels:
    app: slackbot-api-server-stage
spec:
  type: LoadBalancer
  selector:
    app: slackbot-api-server-stage
  ports:
  - protocol: TCP
    port: 443
    targetPort: 443
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slackbot-api-server-stage
  labels:
    app: slackbot-api-server-stage
spec:
  selector:
    matchLabels:
      app: slackbot-api-server-stage
  template:
    metadata:
      labels:
        app: slackbot-api-server-stage
        environment: product
    spec:
      containers:
      - name: slackbot-api-server-stage
        image: vsaninternaltools-docker-local.artifactory.eng.vmware.com/stage/slackbot/slackbot-api-server-stage-stage:1.3
        imagePullPolicy: Always
        env:
        - name: MONGO_DB
          value: slack-bot
        - name: MONGO_PORT
          value: "27017"
        - name: PORT
          value: "443"
        ports:
        - containerPort: 443
        resources:
          requests:
            memory: "200Mi"
            cpu: "100m"
          limits:
            memory: "500Mi"
            cpu: "200m"
        volumeMounts:
        - name: slackbot-nfs
          mountPath: /slackbot/persist
      volumes:
        - name: slackbot-nfs
          nfs:
            server: pa-archive.eng.vmware.com
            path: /slackbot-aas
