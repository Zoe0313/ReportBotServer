apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: slackbot-server.ara.decc.vmware.com
  name: slackbot-api-server
  labels:
    app.name: slackbot-api-server
spec:
  type: LoadBalancer
  selector:
    role: slackbot-api-server
  ports:
  - protocol: TCP
    port: 443
    targetPort: 443
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slackbot-api-server
  labels:
    app.name: slackbot-api-server
spec:
  selector:
    matchLabels:
      app.name: slackbot-api-server
      app: slackbot-api-server
  template:
    metadata:
      labels:
        app.name: slackbot-api-server
        app: slackbot-api-server
        role: slackbot-api-server
        environment: product
    spec:
      containers:
      - name: slackbot-api-server
        image: vsaninternaltools-docker-local.artifactory.eng.vmware.com/stage/slackbot/slackbot-server:1.6
        imagePullPolicy: Always
        env:
        - name: MONGO_DB
          value: slackbot
        - name: MONGO_PORT
          value: "27017"
        - name: PORT
          value: "443"
        ports:
        - containerPort: 443
        resources:
          requests:
            memory: "200Mi"
            cpu: "100m"
          limits:
            memory: "500Mi"
            cpu: "200m"
        volumeMounts:
        - name: slackbot-nfs
          mountPath: /slackbot/persist
      volumes:
        - name: slackbot-nfs
          nfs:
            server: pa-archive.eng.vmware.com
            path: /slackbot-aas
